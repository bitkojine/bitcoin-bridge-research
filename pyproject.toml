[project]
name = "bitcoin-bridge-research"
version = "0.1.0"
description = "Domain-as-code research mapping bridges between traditional finance, law, and Bitcoin"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"

